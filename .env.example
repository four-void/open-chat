# Development database only; bind remains on 127.0.0.1.
POSTGRES_PASSWORD=openchat_local_only
# Production only: generate both values; never commit real secrets.
# DATA_ENCRYPTION_KEY=<openssl rand -base64 32>
# SECRET_KEY_BASE=<mix phx.gen.secret>
# DATABASE_URL=ecto://openchat:password@database-host/openchat_prod
# PHX_HOST=chat.example.com
# TURN_URLS=turn:turn.example.com:3478?transport=udp,turn:turn.example.com:3478?transport=tcp
# TURN_SECRET=<same strong random secret as coturn>
